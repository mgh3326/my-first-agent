[project]
name = "my-first-agent"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.9"
dependencies = [
    "notebook>=7.4.5",
    "openai>=1.108.1",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
]
